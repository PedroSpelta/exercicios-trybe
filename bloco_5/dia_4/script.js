/* eslint-disable require-jsdoc */
const btn = document.querySelector('button');
btn.appendChild('click', changeAll);

function changeAll(event) {
  const bgColor = document.querySelector('background-input');
  const textColor = document.querySelector('textcolor-input');
  const fontSize = document.querySelector('font-size-input');
  const spacing = document.querySelector('spacing-input');
  const fontFamily = document.querySelector('font-family-input');
}
